# VoiceTrigger 使用说明

## 功能概述

VoiceTrigger 是一个基于 TarsosDSP 的语音触发模组，允许玩家录制语音片段作为触发键，并通过说出相应的语音来触发 Minecraft 中的按键操作。

## 核心技术

- **MFCC（梅尔频率倒谱系数）**: 用于提取语音特征
- **DTW（动态时间规整）**: 用于计算不同语速的语音相似度
- **VAD（语音活动检测）**: 通过静音检测优化性能
- **TarsosDSP**: 音频处理库

## 使用方法

### 1. 打开语音绑定界面

- 默认按键：`B`
- 也可以在设置中修改快捷键

### 2. 录制语音

1. 在语音绑定界面中，找到你想绑定语音的按键
2. 点击对应的"录制"按钮
3. 开始说出你的触发词（建议 1-2 秒的清晰发音）
4. 点击"停止"按钮结束录制
5. 系统会自动提取 MFCC 特征并保存

### 3. 使用语音触发

- 进入游戏世界后，语音监听器会自动启动
- 说出之前录制的触发词，系统会自动触发对应的按键
- 退出世界时，监听器会自动停止

## 配置文件

语音配置保存在：`config/voice_trigger/`

- `.vcfg` 文件：MFCC 特征数据
- `.wav` 文件：原始录音文件

## 性能优化

1. **采样率优化**：使用 16kHz 采样率，在保证识别效果的同时减少计算量
2. **MFCC 维度**：使用 13 维 MFCC，平衡识别精度和性能
3. **静音检测**：只在检测到声音时进行特征提取和匹配
4. **异步处理**：DTW 匹配在独立线程中进行，不会阻塞游戏

## 技术参数

- **采样率**: 16000 Hz
- **缓冲区大小**: 1024
- **重叠大小**: 512
- **MFCC 系数**: 13
- **Mel 滤波器**: 40
- **频率范围**: 133 - 8000 Hz
- **静音阈值**: -70 dB
- **相似度阈值**: 0.15（DTW 距离）

## 最佳实践

1. **录制建议**：
   - 在安静的环境中录制
   - 使用清晰、有特色的词语或短语
   - 避免过短（<0.5秒）或过长（>3秒）的录音
   - 发音要清晰、稳定

2. **触发建议**：
   - 尽量使用与录制时相同的语调和语速
   - 在相对安静的环境中使用
   - 不同按键使用不同的触发词，避免混淆

3. **性能建议**：
   - 不要绑定过多的语音触发键（建议 10 个以内）
   - 定期清理不需要的语音配置

## 故障排除

1. **无法录音**：
   - 检查麦克风是否正常工作
   - 确保游戏有麦克风权限

2. **识别不准确**：
   - 调整相似度阈值（修改 `VoiceListener.java` 中的 `SIMILARITY_THRESHOLD`）
   - 重新录制语音
   - 确保环境噪音较小

3. **触发延迟**：
   - 正常情况下延迟应在 0.5 秒内
   - 如果延迟过高，检查系统性能

## 开发者信息

### 架构说明

```
dev.dubhe.voice.audio/
├── DTW.java                    # DTW 算法实现
├── MFCCExtractor.java          # MFCC 特征提取
├── VoiceListener.java          # 实时语音监听
├── VoiceRecorder.java          # 语音录制
└── VoiceProfileManager.java    # 配置文件管理
```

### 扩展开发

如需修改识别参数，可以调整以下常量：

- `MFCCExtractor.SAMPLE_RATE`: 采样率
- `MFCCExtractor.MFCC_COUNT`: MFCC 维度
- `VoiceListener.SIMILARITY_THRESHOLD`: 匹配阈值
- `VoiceListener.WINDOW_SIZE`: 滑动窗口大小

## 许可证

GNU LGPL 3.0
